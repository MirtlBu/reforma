(function() {
    console.log('init');
    // var labels = document.getElementsByClassName('label__item'),
    //     inputs = document.getElementsByClassName('input');
    // function showInput(e) {
    //     var input_id = e.target.getAttribute('for');
    //     Array.prototype.forEach.call(inputs, function(v, i) {
    //         v.className = 'input';
    //     });
    //     document.getElementById(input_id).className = 'input input--active';
    // }
    // Array.prototype.forEach.call(labels, function(v, i) {
    //         v.addEventListener("click", showInput);
    //     });
})()